spring.application.name=defiance-spring
server.port=${PORT:8080}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/defiance_spring}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

app.frontend-base-url=${APP_FRONTEND_BASE_URL:http://localhost:3000}
app.contact-email=${APP_CONTACT_EMAIL:DefianceColivefree@gmail.com}

spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=a04d43001@smtp-brevo.com
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.mail.from=DefianceCo <defiancecolivefree@gmail.com>

brevo.api.key=${BREVO_API_KEY}
